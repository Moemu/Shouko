.venv/
.venv-gpu/
vendor/
data/
node_modules/
web/dist/
__pycache__/

# 训练产物与运行日志：体积大，不入库
runs/**/*.pt
runs/**/*.pt.bak
runs/**/*.npz
runs/**/*.tgz
runs/**/*.log
runs/**/*.pid
runs/**/*.jsonl
runs/**/brain_activity.json

web/public/avatar.vrm
web/public/yumi.vrm

# 云端目标（私有 SSH 端点），仅本机保存
runs/cloud/target.json

# Optional third-party reference cache; never publish downloaded source copies.
/.cache/references/

# 浏览器预览权重包：由 app/export_preview_weights.py 生成（AGENTS.md 常用命令与
# PREVIEW.md §1 都要求社区用户跑这一步），体积大，发布走 Release（红线 2/8）
artifacts/

# 本仓库 .gitignore 的纪律：只写「社区用户确会产生」的产物。仅由本机特定状态产生的
# 文件放 .git/info/exclude（不随仓库分发），不要写在这里。
