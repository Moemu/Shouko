# 本地发布候选验证

日期：2026-09-23。只做打包和推理检查，没有继续训练，没有创建远程Release。

- 候选文件：`runs/releases/v0.2.0-candidate/shouko-v0.2.0-candidate.tgz`。
- 大小：641,569,393字节。
- SHA-256：`358f1fecfa69d3d734f232e7a07d2d460a3d4cc3be7e0c86a6b42140d33e8510`。
- 内容：44个白名单文件和内嵌manifest。逐文件大小、SHA与归档总SHA均已核验。
- 模型SHA与最终留出记录匹配；包内sim、full_brain、ordered_inference、evaluate_locomotion及身体XML与实测评估记录中的源码哈希相同。
- 四份权重的元数据经过私有主机和个人路径检查。白名单不包含SSH配置、原始会话、Yumi VRM或旧PPO状态。

## 单环境运行

为避免通过慢速上传链路重复传输同一大文件，云端测试目录从候选包解出源码和元数据，再复制云端已有的四份相同权重与相同图数据。它们是独立实体文件，不是指向原工作区的软链接。`smoke.py` 在运行前对全部44个文件逐项核对候选manifest，所以测试目录与本地候选包的有效载荷一致。未将中断的完整上传文件当作完成包。

复用云端现有Python依赖环境，没有宣称全新pip安装已经实测。独立目录内不存在vendor、VRM或私有连接配置，推理没有读取教师。

结果见 `runs/releases/v0.2.0-candidate/package_smoke.json`：主模型 `f1a20071…`，原生CSR，batch=1，seed=4242，目标0.5m/s，30秒无跌倒且严格通过；均速0.527065m/s，终点横移0.877427m。此项只验证包完整性与单环境执行，不替代此前四组留出，不构成新泛化或UI验收。

脚本编译和 `git diff --check` 均通过。构建脚本会拒绝覆盖已存在的候选目录，并拒绝与评估源码或固定权重哈希不符的输入。

## 发布状态

本地候选可供审阅。正式发布仍需把实际代码和文档归入一致的发行提交，绑定最终tag与源码，并按用户确定的版本安排上传。若改动包内任一文件，必须生成新候选和新哈希，不沿用这里的验证结论。

当前候选可用于无界面推理/评估。全新环境依赖安装、浏览器预览默认切换及新评估格式的界面接入尚未验证。它们与模型已通过四组步态留出是不同事项。

## 后续发行集成验证

用户随后授权正式执行。主模型已设为默认 Yumi 预览，旧默认权重、记录及优化器状态已归档。前端显示四组严格成绩和限制，服务同时校验权重哈希与物理接口。中英文切换、继续、暂停与重置在本机浏览器实测；角色与脑活动画面正常，浏览器未记录 error。新评估转换测试覆盖合格摆动缺失、错权重、错身体、错接口和未完成记录。

最终包使用 `shouko-v0.2.0.tgz`，不复用上方候选SHA。构建器要求运行源码和发行说明逐字节匹配发行提交，并重新校验全部文件。最后上传附件的SHA以Release的 `SHA256SUMS.txt` 为准。候选的推理核心、身体与权重保持不变；新增的评估适配器和安装说明另经集成验证。

额外从暂存源码树导出干净目录，使用公开安装脚本安装同哈希模型与证据。该目录没有 vendor、VRM、私有端点或原始 runs；复用既有 Python 依赖，服务成功加载主模型、显示 63/63，缺失角色时自动显示物理骨架。此检查补充了社区文件依赖验证，不等同于全新 pip 安装。
