# 本轮进展与提交交接（2026-09-22）

## 当前状态

本轮已完成云端输入学习诊断、在线尺度 A/B、反馈消融、评估重复性校准和命令响应试验。持续目标以交付路线决策结束，尚未取得可靠行走突破。没有替换生产模型或启动下一轮课程训练。

当前决策是保留完整连接组和 Yumi 身体，下一阶段优先验证正负初始朝向的恢复训练，并用两个训练种子及原生多指标评估决定是否扩大投入。具体配置和停止门槛见 [最终路线决策与执行门槛](../proposals/2026-09-22/Codex-最终路线决策与执行门槛.md)。

## 已完成的代码工作

- 价值网络的输入均值、标准差独立于 actor 保存及恢复。旧 PPO 状态使用修改前的来源统计，改变新增 actor 输入尺度不再同时改变价值网络坐标。
- 新输入尺度只能用于尚未学习的零初始化列；检查维数、正数和有限值，拒绝静默重缩放已学习列。
- 输入学习探针支持指定种子，记录分组梯度和单独参数组更新对动作的影响。
- 新增固定权重反馈消融脚本，包含来源阴性对照、正常重复、运行预算和 checkpoint 哈希检查。
- 实验目录保存固定顺序 CSR 推理及原生 MuJoCo 对照脚本。这些仍是诊断实现，未接入生产训练或预览。

## 实测结论

| 工作 | 主要发现 | 记录 |
|---|---|---|
| 输入学习诊断 | 新输入可传播并得到梯度；当前四步展开可达 85.01% 感觉输入行 | [输入尺度诊断](INPUT_SCALE_CLOUD_20260922.md) |
| 在线尺度 A/B | B 的局部动作敏感度提高约 17–31 倍，但未获得稳定行走收益 | [A/B 记录](ACTOR_SCALE_AB_20260922.md) |
| 数值重复性 | 原 GPU 路径同条件长轨迹存在波动；固定顺序 CSR 加原生物理的来源重复及阴性对照轨迹哈希一致 | [路线诊断](ROUTE_DECISION_20260922.md) |
| 27 初态比较 | 来源成功 2/27，B 成功 6/27；B 平均横移更大，未晋升 | 同上 |
| 命令配对 | 速度端点配对 9/9 正响应，转向配对 25/27 正响应；跟踪仍不可靠 | 同上 |
| 转向限幅 | 上限 0.2/0.4/0.8 对应成功 2/3/5，扩大上限未改善方向通过数 | 同上 |

这些结果支持改变训练与选择方法，不能证明现有结构必然成功或不可能成功。精确重复也不能代替初态和独立训练种子的泛化检验。

## 提案状态

同日各 Agent 提案按作者原文保留，见 [提案索引](../proposals/README.md)。新收到的 [Qoder 复核与优先级重排](../proposals/2026-09-22/Qoder-路线决策复核与优先级重排.md) 建议提前进行奖励和小策略对照；它属于待讨论建议，未执行，也不自动改写已经记录的路线决策。本次整理不扩大为新一轮方向审议。

## 提交前验证

在云端现有隔离工作区、禁用 CUDA 的条件下运行以下小图 CPU 回归，全部通过；相关源码 SHA-256 与本地一致：

- `python -m app.test_value_observation`：旧状态恢复、actor/critic 隔离、保存恢复及下一次 Adam 更新一致。
- `python -m app.test_observation_probe`：价值函数保持、零列 actor 等价、优化器学习率恢复及尺度探针。
- `test_feedback.py`：仅修改新增观测，调用者输入和权重不变，零权重通道阴性对照等价。

本轮实测结果和备份验证见各 `runs/` 实验目录。提交仅包含源码、JSON 小型证据、清单和文档；权重、压缩归档、日志、连接组数据及 VRM 继续由 `.gitignore` 排除。已检查待提交文件的隐私信息、大文件及 JSON 格式。

## 后续工作与实例

下一轮先整理可复用的原生评估入口，再实施朝向初态覆盖对照。课程训练、最终独立 30 秒验收、120 秒长走、扰动及断连检查均未在本轮完成。

实例按用户要求持续保留；本次整理只运行 CPU 回归，没有启动 GPU 训练、关闭实例、释放 GPU 或切换无卡模式。
