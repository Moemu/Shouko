# PPO8 修复与重启记录：2026-09-15 22:35

## 背景

- ppo5：首个云端 PPO，best = 第 65 轮（score 0.721，16/32），后退化。验收见 PPO5_REVIEW_20260915.md。
- ppo6（PID 4303）：21:17 从 iter65_baseline 重启，代码已含航向闭环指令对齐与 KL 诊断，但奖励结构未改。评估显示 30 秒存活满分与深度负分并存（横向漂移/倒走），750–795 轮出现 reward hacking。
- ppo7（22:2x 启动）：含奖励重塑，但 5 轮内策略崩溃（12/32 → 0/32）。确诊：全新 value 网络 + 噪声优势 + PPO 更新，好策略被瞬间破坏。已终止。

## ppo8 的代码改动（app/ppo_yumi.py，已同步云端并启动）

1. **奖励重塑**（在 ppo7 基础上保留）：前向速度、横向速度、航向三个指数项独立加权（1.5 / 0.6 / 0.8），直立 0.3、存活 0.1，避免漂移被存活项补贴。
2. **value 网络预热**：`--value-warmup 8`（默认 8 轮）——前 8 轮冻结策略只训 value（双倍 epochs），防止新 value 的噪声优势毁掉基线策略。
3. **Adam 状态进 ppo_state**：`ppo_state.pt` / `ppo_state_best.pt` 现包含 value、log_std、两个 Adam 优化器状态；`--resume-state` 恢复时日志标注是否含 optimizer。

既有改动（其他 Agent 完成，本次保留）：采集用航向闭环指令（与验收一致）、approx_kl/clipfrac 日志、KL 早停（1.5×0.03）、lr 线性衰减、best.pt 历史最高分保护、ppo_state_best 与 best 配对。

## ppo8 启动参数

```
.venv/bin/python -u -m app.ppo_yumi --max-seconds 15600 --eval-every 5 \
  --resume runs/yumi/iter65_baseline.pt    # 无 --resume-state：全新 value/log_std/Adam
```
PID 见云端 `runs/yumi/train.pid`（6648），日志 `ppo8.log`。约 22:35 + 4.3 h 结束。

## 结果（2026-09-15 23:1x 最终）

**ppo9 第 10 轮即达 score 0.853（31/32）成为最终 best.pt**，后续 175 轮未再超越；该检查点通过 16/16 完整验收（见 YUMI_BODY.md 最终状态）。本节下方"早期信号"为当时记录，其中"第 45 轮 0.775"并非最终最佳。

## ppo9 早期信号（当时记录）

- 基线评估 9/32（score 0.537）；历史最佳 0.721 受保护不被覆盖。
- 预热 8 轮期间策略未动（pi_loss=0，approx_kl=0）。
- 解冻后第 5 轮评估 16/32、score 0.623、30 秒满存活——预热有效，策略未被破坏。
- 仍有波动（第 15 轮跌至 5.1 s 后恢复）；successes 在 30 秒满存活时仍可能很低——success 要求速度误差 ≤0.15 且横漂达标，score 与 successes 读数需区分。

## 下一步

- 观察 score 是否超过 0.721；超过后 best.pt 自动更新（配套 ppo_state_best.pt）。
- 达标后对 best.pt 跑 `app.evaluate_full --robot yumi` 完整验收（新随机种子，见 PPO5_REVIEW 第 5 条）。
